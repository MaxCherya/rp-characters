export type LoginFormValues = {
    username: string;
    password: string;
};

export type RegistrationFormValues = {
    username: string;
    email: string;
    password: string;
    password2: string;
};